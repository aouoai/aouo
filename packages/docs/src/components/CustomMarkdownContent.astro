---
const { hasSidebar } = Astro.locals.starlightRoute;
---

{
  hasSidebar ? (
    <div class="markdown-wrapper">
      <div class="sl-markdown-content">
        <slot />
      </div>
    </div>
  ) : (
    <slot />
  )
}
